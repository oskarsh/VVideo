/**
 * Shared UI class names aligned with docs/STYLE-SYSTEM.md.
 * Use these so styling stays consistent and one place to change.
 */

/** Standard text/number input (sidebar panels). */
export const inputClass =
  'block w-full mt-0.5 px-1.5 py-1 rounded bg-black/30 border border-white/10 text-xs text-white/80 focus:border-white/30 outline-none'

/** Section heading (e.g. "Background", "Bezier"). Optional: add mb-2 or py-2.5. */
export const sectionHeadingClass =
  'text-xs font-semibold text-white/60 uppercase tracking-wider'

/** Small label above a single control. */
export const smallLabelClass =
  'text-[10px] text-white/50 uppercase tracking-wider block mb-1.5'
